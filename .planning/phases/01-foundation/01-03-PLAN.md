---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - .devcontainer/devcontainer.json
  - .devcontainer/docker-compose.yml
  - .devcontainer/Dockerfile
  - .vscode/extensions.json
  - .vscode/settings.json
autonomous: true
requirements:
  - TOOL-07
  - TOOL-08
must_haves:
  truths:
    - Dev container configuration enables zero-setup contributions
    - VS Code extensions are pre-configured for Python, Ruff, ESLint, Tailwind, Prettier
    - Dev container includes Python 3.13, Node.js 20+, PostgreSQL 15
    - Post-create command runs just setup automatically
    - Ports 3000, 8000, 5432 are forwarded
  artifacts:
    - path: .devcontainer/devcontainer.json
      provides: Dev container configuration
      contains: "postCreateCommand", "forwardPorts", "customizations"
    - path: .devcontainer/docker-compose.yml
      provides: Docker Compose with PostgreSQL
      contains: "postgres:15", "app:", "db:"
    - path: .devcontainer/Dockerfile
      provides: Development environment image
      contains: "python:3.13", "uv", "just"
    - path: .vscode/extensions.json
      provides: Recommended VS Code extensions
      contains: "ms-python.python", "charliermarsh.ruff"
    - path: .vscode/settings.json
      provides: VS Code settings for formatting and linting
      contains: "editor.formatOnSave", "ruff.organizeImports"
  key_links:
    - from: .devcontainer/devcontainer.json
      to: Justfile
      via: "postCreateCommand": "just setup"
    - from: .devcontainer/devcontainer.json
      to: .devcontainer/docker-compose.yml
      via: "dockerComposeFile": "docker-compose.yml"
    - from: .vscode/settings.json
      to: Ruff extension
      via: "editor.defaultFormatter": "charliermarsh.ruff"
---

<objective>
Create dev container configuration for zero-setup contributions and VS Code settings with pre-configured extensions.

Purpose: Enable contributors to start development immediately via GitHub Codespaces or VS Code Dev Containers without local setup.
Output: Complete .devcontainer/ directory with Docker Compose, Dockerfile, and devcontainer.json, plus .vscode/ directory with extensions and settings.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create dev container configuration</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-03 dev container scope)
    - .planning/phases/01-foundation/01-RESEARCH.md (dev container patterns)
    - Justfile (to reference in postCreateCommand)
  </read_first>
  <files>.devcontainer/devcontainer.json, .devcontainer/docker-compose.yml, .devcontainer/Dockerfile</files>
  <action>
    Create the .devcontainer/ directory with three files:

    1. .devcontainer/Dockerfile:
    ```dockerfile
    FROM mcr.microsoft.com/devcontainers/python:3.13

    # Install Node.js 20
    RUN curl -fsSL https://deb.nodesource.com/setup_20.x | bash - \
        && apt-get install -y nodejs

    # Install UV
    RUN curl -LsSf https://astral.sh/uv/install.sh | sh \
        && mv /root/.local/bin/uv /usr/local/bin/uv \
        && mv /root/.local/bin/uvx /usr/local/bin/uvx

    # Install Just
    RUN curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash - --to /usr/local/bin

    # Set working directory
    WORKDIR /workspace

    # Keep container running
    CMD ["sleep", "infinity"]
    ```

    2. .devcontainer/docker-compose.yml:
    ```yaml
    version: '3.8'

    services:
      app:
        build:
          context: ..
          dockerfile: .devcontainer/Dockerfile
        volumes:
          - ..:/workspace:cached
        command: sleep infinity
        depends_on:
          - db
        environment:
          - DATABASE_URL=postgresql://clerk:clerk@db:5432/clerk

      db:
        image: postgres:15
        environment:
          POSTGRES_USER: clerk
          POSTGRES_PASSWORD: clerk
          POSTGRES_DB: clerk
        ports:
          - "5432:5432"
        volumes:
          - postgres_data:/var/lib/postgresql/data

    volumes:
      postgres_data:
    ```

    3. .devcontainer/devcontainer.json:
    ```json
    {
      "name": "Clerk Development",
      "dockerComposeFile": "docker-compose.yml",
      "service": "app",
      "workspaceFolder": "/workspace",
      "features": {
        "ghcr.io/devcontainers/features/common-utils:2": {
          "installZsh": true,
          "configureZshAsDefaultShell": true
        },
        "ghcr.io/devcontainers/features/node:1": {
          "version": "20"
        }
      },
      "postCreateCommand": "just setup",
      "forwardPorts": [3000, 8000, 5432],
      "portsAttributes": {
        "3000": {
          "label": "Frontend",
          "onAutoForward": "notify"
        },
        "8000": {
          "label": "Backend API",
          "onAutoForward": "notify"
        },
        "5432": {
          "label": "PostgreSQL",
          "onAutoForward": "silent"
        }
      },
      "customizations": {
        "vscode": {
          "extensions": [
            "ms-python.python",
            "charliermarsh.ruff",
            "dbaeumer.vscode-eslint",
            "bradlc.vscode-tailwindcss",
            "esbenp.prettier-vscode"
          ],
          "settings": {
            "python.defaultInterpreterPath": "/usr/local/bin/python",
            "python.analysis.typeCheckingMode": "strict"
          }
        }
      },
      "remoteUser": "vscode"
    }
    ```
  </action>
  <verify>
    <automated>test -d .devcontainer && test -f .devcontainer/devcontainer.json && test -f .devcontainer/docker-compose.yml && test -f .devcontainer/Dockerfile && grep -q "postCreateCommand" .devcontainer/devcontainer.json && grep -q "just setup" .devcontainer/devcontainer.json && grep -q "forwardPorts" .devcontainer/devcontainer.json && grep -q "postgres:15" .devcontainer/docker-compose.yml</automated>
  </verify>
  <done>Dev container configured with Python 3.13, Node.js 20, PostgreSQL 15, automatic setup via just setup</done>
  <acceptance_criteria>
    - .devcontainer/devcontainer.json exists
    - .devcontainer/devcontainer.json contains "postCreateCommand": "just setup"
    - .devcontainer/devcontainer.json contains "forwardPorts": [3000, 8000, 5432]
    - .devcontainer/devcontainer.json contains "dockerComposeFile": "docker-compose.yml"
    - .devcontainer/devcontainer.json references "ms-python.python" extension
    - .devcontainer/docker-compose.yml exists with "postgres:15" service
    - .devcontainer/docker-compose.yml contains DATABASE_URL environment variable
    - .devcontainer/Dockerfile exists with Python 3.13 base image
    - .devcontainer/Dockerfile installs UV and Just
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create VS Code settings and extensions</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-06 VS Code configuration)
    - .planning/phases/01-foundation/01-RESEARCH.md (VS Code settings pattern)
  </read_first>
  <files>.vscode/extensions.json, .vscode/settings.json</files>
  <action>
    Create the .vscode/ directory with two files:

    1. .vscode/extensions.json:
    ```json
    {
      "recommendations": [
        "ms-python.python",
        "charliermarsh.ruff",
        "dbaeumer.vscode-eslint",
        "bradlc.vscode-tailwindcss",
        "esbenp.prettier-vscode"
      ]
    }
    ```

    2. .vscode/settings.json:
    ```json
    {
      "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
      "python.analysis.typeCheckingMode": "strict",
      "ruff.organizeImports": true,
      "ruff.fixAll": true,
      "editor.formatOnSave": true,
      "editor.codeActionsOnSave": {
        "source.fixAll.ruff": "explicit",
        "source.organizeImports.ruff": "explicit"
      },
      "[python]": {
        "editor.defaultFormatter": "charliermarsh.ruff",
        "editor.formatOnSave": true
      },
      "[typescript]": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      },
      "[typescriptreact]": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      },
      "[javascript]": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      },
      "[javascriptreact]": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true
      },
      "files.exclude": {
        "**/__pycache__": true,
        "**/.pytest_cache": true,
        "**/.mypy_cache": true,
        "**/.ruff_cache": true,
        "**/.venv": true,
        "**/node_modules": true
      },
      "search.exclude": {
        "**/__pycache__": true,
        "**/.pytest_cache": true,
        "**/.mypy_cache": true,
        "**/.ruff_cache": true,
        "**/.venv": true,
        "**/node_modules": true,
        "**/dist": true
      }
    }
    ```
  </action>
  <verify>
    <automated>test -d .vscode && test -f .vscode/extensions.json && test -f .vscode/settings.json && grep -q "ms-python.python" .vscode/extensions.json && grep -q "charliermarsh.ruff" .vscode/extensions.json && grep -q "editor.formatOnSave" .vscode/settings.json && grep -q "charliermarsh.ruff" .vscode/settings.json</automated>
  </verify>
  <done>VS Code configured with Python, Ruff, ESLint, Tailwind, Prettier extensions and format-on-save settings</done>
  <acceptance_criteria>
    - .vscode/extensions.json exists
    - .vscode/extensions.json contains "ms-python.python"
    - .vscode/extensions.json contains "charliermarsh.ruff"
    - .vscode/extensions.json contains "dbaeumer.vscode-eslint"
    - .vscode/extensions.json contains "bradlc.vscode-tailwindcss"
    - .vscode/extensions.json contains "esbenp.prettier-vscode"
    - .vscode/settings.json exists
    - .vscode/settings.json contains "editor.formatOnSave": true
    - .vscode/settings.json contains "python.analysis.typeCheckingMode": "strict"
    - .vscode/settings.json contains "editor.defaultFormatter": "charliermarsh.ruff" for Python
  </acceptance_criteria>
</task>

</tasks>

<verification>
- TOOL-07: Dev container configured with Python 3.13, Node.js 20+, PostgreSQL 15, just setup post-create command
- TOOL-08: VS Code settings and extensions configured for Python, Ruff, ESLint, Tailwind, Prettier
</verification>

<success_criteria>
- `test -f .devcontainer/devcontainer.json && grep -q "just setup" .devcontainer/devcontainer.json`
- `grep -q "forwardPorts" .devcontainer/devcontainer.json`
- `test -f .devcontainer/docker-compose.yml && grep -q "postgres:15" .devcontainer/docker-compose.yml`
- `test -f .devcontainer/Dockerfile && grep -q "python:3.13" .devcontainer/Dockerfile`
- `test -f .vscode/extensions.json && grep -q "charliermarsh.ruff" .vscode/extensions.json`
- `test -f .vscode/settings.json && grep -q "editor.formatOnSave" .vscode/settings.json`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
