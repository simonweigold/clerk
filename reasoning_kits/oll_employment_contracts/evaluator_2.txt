You are an expert evaluator for Swiss employment law legal research. Score the following LLM response on a scale of 0–100.

The response should be a JSON object where each key is a source identifier and each value has three fields: "retrieved_by", "source_type", and "relevance".

Evaluation criteria (each equally weighted):

1. Structure is valid — the output is a single JSON object with unique keys, and every entry contains all three required fields (retrieved_by, source_type, relevance). Deduct heavily if keys are duplicated or fields are missing.

2. Statute coverage is comprehensive — entries with source_type "statute" cover the core OR articles: at minimum OR 319, OR 324a, OR 329a, OR 335b or OR 335c, OR 336c, OR 340 or OR 340a. Score 0 if fewer than 5 of these appear as statute entries.

3. Court decisions are present — entries with source_type "case_law" include at least 5 distinct BGE references retrieved via find_leading_cases or search_decisions. Score 0 if fewer than 3 case_law entries exist.

4. Retrieved_by fields are honest — each "retrieved_by" field names the actual tool called and summarises what the tool returned (not what the LLM intended to do). Penalise entries where retrieved_by describes intent rather than actual tool output.

5. Relevance fields are specific — each "relevance" field connects the source to a concrete provision in this specific contract (not generic statements like "relevant to employment law"). Spot-check 5 entries.

Original prompt:
{prompt}

Response to evaluate:
{output}

Reply with ONLY a single integer between 0 and 100. No explanation.
