---
phase: 02-contributor-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/pr-checks.yml
  - BRANCH_PROTECTION.md
autonomous: true
requirements:
  - GHUB-03
  - GHUB-04
  - GHUB-05
  - GHUB-06
must_haves:
  truths:
    - Pull requests trigger automated test runs
    - Pull requests trigger automated lint checks
    - Branch protection rules documented for maintainers
    - At least 3 issues labeled "good first issue" exist
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow for tests and linting"
      exports: []
    - path: ".github/workflows/pr-checks.yml"
      provides: "PR-specific checks workflow"
      exports: []
    - path: "BRANCH_PROTECTION.md"
      provides: "Documentation for branch protection setup"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Justfile"
      via: "just test and just lint commands"
---

<objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-contributor-experience/02-CONTEXT.md
@Justfile (to confirm exact command names)
@packages/clerk/pyproject.toml (for Python versions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-03 for CI specifications)
    - Justfile (confirm just test and just lint commands)
    - packages/clerk/pyproject.toml (Python version constraints)
  </read_first>
  <action>Create `.github/workflows/ci.yml` with test and lint jobs per D-03.

Structure:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          version: "0.4.x"
      - name: Run lint
        run: just lint

  test:
    strategy:
      matrix:
        python-version: ['3.12', '3.13']
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          version: "0.4.x"
      - name: Run tests
        run: just test
```

Check pyproject.toml requires-python - if it says >=3.13, use only ['3.13'] for matrix. If it allows 3.12, use both.

Use astral-sh/setup-uv@v3 for UV installation (modern, fast).
Both jobs must use `just` commands - ensure the Justfile is available at repo root.
  </action>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists
    - Contains "name: CI"
    - Triggers on push to main and pull_request to main
    - Has lint job using ubuntu-latest
    - Has test job with matrix strategy (Python 3.12/3.13, ubuntu/macOS)
    - Uses astral-sh/setup-uv@v3
    - Calls `just lint` and `just test`
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "name: CI" .github/workflows/ci.yml && grep -q "astral-sh/setup-uv" .github/workflows/ci.yml && grep -q "just test" .github/workflows/ci.yml && grep -q "just lint" .github/workflows/ci.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CI workflow exists with lint and test jobs using UV and Just commands</done>
</task>

<task type="auto">
  <name>Task 2: Create branch protection documentation</name>
  <files>BRANCH_PROTECTION.md</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-05 for protection rules)
    - .github/workflows/ci.yml (to reference exact check names)
  </read_first>
  <action>Create `BRANCH_PROTECTION.md` documenting branch protection rules for maintainers per D-05.

Content:
```markdown
# Branch Protection Setup

This document describes the branch protection rules for the `main` branch.

## Required Settings

In GitHub Repository Settings → Branches → Branch protection rules:

### Branch name pattern
```
main
```

### Protect matching branches

- [x] **Require a pull request before merging**
  - Require approvals: 1
  - Dismiss stale PR approvals when new commits are pushed
  - Require review from CODEOWNERS (if CODEOWNERS file exists)

- [x] **Require status checks to pass before merging**
  - Require branches to be up to date before merging
  - Status checks that are required:
    - `lint` (from CI workflow)
    - `test` (from CI workflow)

- [x] **Require conversation resolution before merging**

- [x] **Do not allow bypassing the above settings**

### Optional Settings

- [ ] **Allow force pushes**
  - Enable "Specify who can force push" → "Repository admins"

- [ ] **Allow deletions**
  - Leave unchecked

## Enabling Protection

1. Go to https://github.com/{owner}/{repo}/settings/branches
2. Click "Add rule"
3. Enter "main" in Branch name pattern
4. Check the settings above
5. Click "Create"

## Verification

After enabling:
1. Create a test PR
2. Verify that:
   - PR requires at least 1 approval
   - CI checks (lint, test) run automatically
   - Merge is blocked until checks pass
```

The exact status check names (`lint` and `test`) come from the job names in ci.yml.
  </action>
  <acceptance_criteria>
    - File `BRANCH_PROTECTION.md` exists
    - Contains "Require a pull request before merging"
    - Mentions "Require approvals: 1"
    - Lists "lint" and "test" as required status checks
    - Mentions "Do not allow bypassing"
    - Contains instructions for enabling in GitHub settings
  </acceptance_criteria>
  <verify>
    <automated>test -f BRANCH_PROTECTION.md && grep -q "Require a pull request" BRANCH_PROTECTION.md && grep -q "lint" BRANCH_PROTECTION.md && grep -q "test" BRANCH_PROTECTION.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Branch protection documentation exists with exact settings per D-05</done>
</task>

<task type="auto">
  <name>Task 3: Create good first issue templates</name>
  <files>.github/GOOD_FIRST_ISSUES.md</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-06 for the 3 issue types)
  </read_first>
  <action>Create `.github/GOOD_FIRST_ISSUES.md` with 3 ready-to-create issues per D-06.

This file will serve as templates for creating the actual GitHub issues. Each issue should have a clear title, description, and acceptance criteria.

Content structure:
```markdown
# Good First Issues

Templates for initial "good first issue" labels. Create these in GitHub Issues.

## Issue 1: Documentation Improvements

**Title:** Improve README with better installation examples

**Labels:** `good first issue`, `documentation`

**Description:**
```
The current README installation section could be clearer for new contributors.

### Task
- Review the README.md installation instructions
- Add more explicit examples for common setups
- Ensure the 5-minute setup claim is accurate

### Acceptance Criteria
- [ ] Installation instructions tested on clean system
- [ ] Examples cover macOS and Linux
- [ ] Troubleshooting section added for common issues
- [ ] Instructions work with the devcontainer setup
```

## Issue 2: Add Test Coverage

**Title:** Add tests for reasoning kit loader

**Labels:** `good first issue`, `testing`

**Description:**
```
The reasoning kit loader (`packages/clerk/src/openclerk/loader.py`) needs better test coverage.

### Task
- Add unit tests for `load_reasoning_kit()` function
- Test edge cases: missing files, invalid JSON, malformed kits
- Aim for >80% coverage on loader module

### Acceptance Criteria
- [ ] Tests exist in `packages/clerk/tests/test_loader.py`
- [ ] All happy paths tested
- [ ] Edge cases (FileNotFoundError, JSONDecodeError) tested
- [ ] Coverage report shows >80% for loader.py
```

## Issue 3: Fix Minor Lint Issues

**Title:** Fix remaining mypy type annotation warnings

**Labels:** `good first issue`, `good first issue`, `refactoring`

**Description:**
```
There are several minor type annotation issues that mypy reports.

### Task
- Run `just lint` to see current warnings
- Fix type annotation issues in non-critical files
- Focus on files in `packages/clerk/src/openclerk/` (not external deps)

### Acceptance Criteria
- [ ] `just lint` passes with no mypy errors
- [ ] No `# type: ignore` comments added (fix properly instead)
- [ ] All changes are type-only (no runtime behavior changes)
```

## Creating These Issues

1. Go to https://github.com/{owner}/{repo}/issues/new
2. Copy title and description from above
3. Add labels: `good first issue` plus relevant category label
4. Repeat for all 3 issues
```

Replace {owner}/{repo} with actual values if known (openclerk/clerk or similar).
  </action>
  <acceptance_criteria>
    - File `.github/GOOD_FIRST_ISSUES.md` exists
    - Contains exactly 3 issue templates
    - Each has a clear title
    - Each has acceptance criteria checklist
    - Each is labeled as "good first issue" appropriate
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/GOOD_FIRST_ISSUES.md && grep -c "## Issue" .github/GOOD_FIRST_ISSUES.md | grep -q "3" && grep -q "good first issue" .github/GOOD_FIRST_ISSUES.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Good first issue templates documented with 3 clear entry-level contributions</done>
</task>

</tasks>

<verification>
- CI workflow triggers on PRs and pushes to main
- CI uses UV (astral-sh/setup-uv) and Just commands
- Matrix includes Python 3.12/3.13 and ubuntu/macOS per D-03
- Branch protection doc references exact CI job names
- Good first issue templates cover docs, tests, and lint fixes per D-06
</verification>

<success_criteria>
- [ ] CI workflow exists at `.github/workflows/ci.yml`
- [ ] CI runs `just test` and `just lint` on Python 3.12/3.13
- [ ] Branch protection documentation exists at `BRANCH_PROTECTION.md`
- [ ] Good first issue templates exist at `.github/GOOD_FIRST_ISSUES.md`
- [ ] All artifacts follow D-03, D-05, D-06 specifications
</success_criteria>

<output>
After completion, create `.planning/phases/02-contributor-experience/02-03-SUMMARY.md`
</output>
