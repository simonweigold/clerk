---
phase: 02-contributor-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - GHUB-01
  - GHUB-02
must_haves:
  truths:
    - Contributors can open bug reports with structured form
    - Contributors can open feature requests with structured form
    - Pull requests show checklist reminding contributors of requirements
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug report form with fields for description, reproduction, expected behavior, environment"
      exports: []
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Structured feature request form with fields for description, use case, proposed solution"
      exports: []
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR checklist referencing just test, just lint, documentation, linked issues"
      exports: []
  key_links: []
---

<objective>
Create GitHub issue templates and pull request template per user decisions D-01 and D-02.

Purpose: Enable structured bug reports and feature requests; ensure PRs include quality checklist.
Output: Two issue templates (YAML forms) and one PR template (markdown).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-contributor-experience/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@Justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bug report issue template</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-01 for template requirements)
  </read_first>
  <action>Create `.github/ISSUE_TEMPLATE/bug_report.yml` as a GitHub issue form (YAML, not markdown).

Required fields per D-01:
- name: "Bug Report"
- description: "Create a report to help us improve"
- title: "[Bug]: "
- labels: ["bug"]

Body sections (use type: textarea unless noted):
1. Description — "A clear description of the bug"
2. Reproduction Steps — "Steps to reproduce" (numbered list format hint)
3. Expected Behavior — "What you expected to happen"
4. Actual Behavior — "What actually happened"
5. Environment — type: input, label "Environment", description with examples (OS, Python version, Clerk version)
6. Additional Context — "Screenshots, logs, or other context" (optional: required: false)

Use proper YAML structure with name, description, body array. Each body item needs type and attributes (label, description, placeholder as needed).
  </action>
  <acceptance_criteria>
    - File `.github/ISSUE_TEMPLATE/bug_report.yml` exists
    - File contains `name: "Bug Report"`
    - File contains `labels:` with `"bug"`
    - File contains at least 5 body fields (description, reproduction, expected, actual, environment)
    - YAML is valid (no syntax errors)
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug_report.yml && grep -q 'name: "Bug Report"' .github/ISSUE_TEMPLATE/bug_report.yml && grep -q '"bug"' .github/ISSUE_TEMPLATE/bug_report.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Bug report template exists with all required fields and "bug" label</done>
</task>

<task type="auto">
  <name>Task 2: Create feature request issue template</name>
  <files>.github/ISSUE_TEMPLATE/feature_request.yml</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-01)
    - .github/ISSUE_TEMPLATE/bug_report.yml (for consistent structure)
  </read_first>
  <action>Create `.github/ISSUE_TEMPLATE/feature_request.yml` as a GitHub issue form.

Required fields per D-01:
- name: "Feature Request"
- description: "Suggest an idea for this project"
- title: "[Feature]: "
- labels: ["enhancement"]

Body sections:
1. Description — "Clear description of the feature"
2. Problem/Use Case — "What problem does this solve?"
3. Proposed Solution — "How should this work?"
4. Alternatives — "Alternative solutions considered" (optional)
5. Additional Context — "Screenshots, mockups, references" (optional)
  </action>
  <acceptance_criteria>
    - File `.github/ISSUE_TEMPLATE/feature_request.yml` exists
    - File contains `name: "Feature Request"`
    - File contains `labels:` with `"enhancement"`
    - File contains at least 3 body fields (description, problem, proposed solution)
    - YAML is valid
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/feature_request.yml && grep -q 'name: "Feature Request"' .github/ISSUE_TEMPLATE/feature_request.yml && grep -q '"enhancement"' .github/ISSUE_TEMPLATE/feature_request.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Feature request template exists with "enhancement" label</done>
</task>

<task type="auto">
  <name>Task 3: Create pull request template</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-02 for exact checklist)
    - Justfile (to confirm exact command names)
  </read_first>
  <action>Create `.github/PULL_REQUEST_TEMPLATE.md` with the exact checklist from D-02.

Content must include:
- Brief intro sentence about describing changes
- Checklist with these exact items:
  - [ ] Tests pass (`just test`)
  - [ ] Code follows style (`just lint`)
  - [ ] Documentation updated (if needed)
  - [ ] Related issue linked

Add a "Description" section prompting for summary of changes.
Add a "Related Issue" section prompting to use "Fixes #123" or "Closes #123" format.
  </action>
  <acceptance_criteria>
    - File `.github/PULL_REQUEST_TEMPLATE.md` exists
    - File contains "- [ ] Tests pass (`just test`)"
    - File contains "- [ ] Code follows style (`just lint`)"
    - File contains "- [ ] Documentation updated (if needed)"
    - File contains "- [ ] Related issue linked"
    - File contains "Fixes #" or "Closes #" in the related issue section
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/PULL_REQUEST_TEMPLATE.md && grep -q "just test" .github/PULL_REQUEST_TEMPLATE.md && grep -q "just lint" .github/PULL_REQUEST_TEMPLATE.md && grep -q "Related issue" .github/PULL_REQUEST_TEMPLATE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>PR template exists with exact checklist from D-02</done>
</task>

</tasks>

<verification>
- All three template files exist in .github/
- Issue templates use YAML form syntax (not markdown)
- PR template uses markdown checklist syntax
- Labels are correctly set (bug, enhancement)
- PR checklist references exact Justfile commands from Phase 1
</verification>

<success_criteria>
- [ ] Bug report template with structured fields exists at `.github/ISSUE_TEMPLATE/bug_report.yml`
- [ ] Feature request template exists at `.github/ISSUE_TEMPLATE/feature_request.yml`
- [ ] Pull request template exists at `.github/PULL_REQUEST_TEMPLATE.md`
- [ ] All templates follow D-01 and D-02 specifications exactly
</success_criteria>

<output>
After completion, create `.planning/phases/02-contributor-experience/02-01-SUMMARY.md`
</output>
