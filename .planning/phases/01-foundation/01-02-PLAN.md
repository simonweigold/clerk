---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/clerk/pyproject.toml
  - Justfile
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-06
must_haves:
  truths:
    - UV workspace is configured for Python dependency management
    - Ruff is configured in pyproject.toml for linting and formatting
    - pytest is configured with coverage reporting (80% threshold)
    - mypy is configured for type checking in strict mode
    - Just task runner provides commands for test, lint, format, dev
  artifacts:
    - path: packages/clerk/pyproject.toml
      provides: Ruff, pytest, mypy configuration
      contains: "[tool.ruff]", "[tool.pytest]", "[tool.mypy]"
    - path: Justfile
      provides: Task runner with setup, test, lint, format, dev commands
      contains: "just setup", "just test", "just lint", "just format", "just dev"
  key_links:
    - from: Justfile
      to: packages/clerk/pyproject.toml
      via: "cd packages/clerk && ruff check ."
    - from: Justfile
      to: uv commands
      via: "uv sync", "uv pip install"
---

<objective>
Configure Python development tooling: UV workspace (already exists), Ruff linting/formatting, pytest with coverage, mypy type checking, and Just task runner.

Purpose: Provide automated code quality gates and a simple task runner interface for all development workflows.
Output: Fully configured pyproject.toml with all tool settings and a Justfile with common commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@packages/clerk/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Configure Ruff in pyproject.toml</name>
  <read_first>
    - packages/clerk/pyproject.toml (current content)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05 code quality gates)
    - .planning/phases/01-foundation/01-RESEARCH.md (Ruff configuration pattern)
  </read_first>
  <files>packages/clerk/pyproject.toml</files>
  <action>
    Add Ruff configuration to packages/clerk/pyproject.toml after the existing [tool.uv] section. Use exact configuration:

    ```toml
    [tool.ruff]
    target-version = "py313"
    line-length = 100

    [tool.ruff.lint]
    select = [
        "E",   # pycodestyle errors
        "F",   # Pyflakes
        "I",   # isort
        "N",   # pep8-naming
        "W",   # pycodestyle warnings
    ]
    ignore = ["E501"]  # Line too long (handled by formatter)

    [tool.ruff.lint.pydocstyle]
    convention = "google"

    [tool.ruff.format]
    quote-style = "double"
    indent-style = "space"
    ```

    Append this to the end of packages/clerk/pyproject.toml, preserving all existing content.
  </action>
  <verify>
    <automated>grep -q "\[tool.ruff\]" packages/clerk/pyproject.toml && grep -q 'target-version = "py313"' packages/clerk/pyproject.toml && grep -q 'line-length = 100' packages/clerk/pyproject.toml && grep -q "\[tool.ruff.lint\]" packages/clerk/pyproject.toml && grep -q '"E"' packages/clerk/pyproject.toml</automated>
  </verify>
  <done>Ruff configured with Python 3.13 target, 100-character line length, and standard rule set</done>
  <acceptance_criteria>
    - packages/clerk/pyproject.toml contains "[tool.ruff]" section
    - packages/clerk/pyproject.toml contains 'target-version = "py313"'
    - packages/clerk/pyproject.toml contains 'line-length = 100'
    - packages/clerk/pyproject.toml contains "[tool.ruff.lint]" section
    - packages/clerk/pyproject.toml contains rule selectors: "E", "F", "I", "N", "W"
    - packages/clerk/pyproject.toml contains "[tool.ruff.format]" section
    - packages/clerk/pyproject.toml contains 'quote-style = "double"'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Configure pytest and mypy in pyproject.toml</name>
  <read_first>
    - packages/clerk/pyproject.toml (current content after Ruff config)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 testing strategy)
    - .planning/phases/01-foundation/01-RESEARCH.md (pytest and mypy patterns)
  </read_first>
  <files>packages/clerk/pyproject.toml</files>
  <action>
    Add pytest and mypy configuration to packages/clerk/pyproject.toml after the Ruff section. Use exact configuration:

    ```toml
    [tool.pytest.ini_options]
    minversion = "8.0"
    addopts = "-ra -q --strict-markers --cov=openclerk --cov-report=term-missing"
    testpaths = ["tests"]
    asyncio_mode = "auto"

    [tool.coverage.run]
    source = ["src/openclerk"]
    branch = true

    [tool.coverage.report]
    fail_under = 80
    show_missing = true

    [tool.mypy]
    python_version = "3.13"
    strict = true
    warn_return_any = true
    warn_unused_configs = true
    ignore_missing_imports = true
    ```

    Append this to the end of packages/clerk/pyproject.toml, preserving all existing content including the Ruff configuration.
  </action>
  <verify>
    <automated>grep -q "\[tool.pytest.ini_options\]" packages/clerk/pyproject.toml && grep -q 'minversion = "8.0"' packages/clerk/pyproject.toml && grep -q "\[tool.coverage.report\]" packages/clerk/pyproject.toml && grep -q 'fail_under = 80' packages/clerk/pyproject.toml && grep -q "\[tool.mypy\]" packages/clerk/pyproject.toml && grep -q 'strict = true' packages/clerk/pyproject.toml</automated>
  </verify>
  <done>pytest configured with 80% coverage threshold and async mode; mypy configured in strict mode</done>
  <acceptance_criteria>
    - packages/clerk/pyproject.toml contains "[tool.pytest.ini_options]" section
    - packages/clerk/pyproject.toml contains 'minversion = "8.0"'
    - packages/clerk/pyproject.toml contains "--cov=openclerk" in addopts
    - packages/clerk/pyproject.toml contains 'asyncio_mode = "auto"'
    - packages/clerk/pyproject.toml contains "[tool.coverage.report]" section
    - packages/clerk/pyproject.toml contains 'fail_under = 80'
    - packages/clerk/pyproject.toml contains "[tool.mypy]" section
    - packages/clerk/pyproject.toml contains 'strict = true'
    - packages/clerk/pyproject.toml contains 'python_version = "3.13"'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create Justfile with common commands</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-02 setup approach)
    - .planning/phases/01-foundation/01-RESEARCH.md (Justfile pattern)
  </read_first>
  <files>Justfile</files>
  <action>
    Create Justfile in repository root with exact content:

    ```justfile
    set dotenv-load

    # Default recipe - list all commands
    default:
        @just --list

    # Setup development environment
    setup:
        #!/usr/bin/env bash
        echo "Setting up Clerk development environment..."
        uv sync
        cd packages/clerk && uv pip install -e ".[dev]"
        cd apps/website && npm install
        echo "Setup complete! Run 'just dev' to start development."

    # Run all tests with coverage
    test:
        cd packages/clerk && pytest --cov=src/openclerk --cov-report=term-missing

    # Run linting (Ruff + mypy)
    lint:
        cd packages/clerk && ruff check src/ tests/
        cd packages/clerk && mypy src/

    # Format all code
    format:
        cd packages/clerk && ruff format src/ tests/

    # Start full development environment
    dev:
        #!/usr/bin/env bash
        trap "kill %1 %2 2>/dev/null || true" EXIT
        just dev-backend &
        just dev-frontend &
        wait

    # Start backend only
    dev-backend:
        cd packages/clerk && uv run clerk web --host 0.0.0.0

    # Start frontend only
    dev-frontend:
        cd apps/website && npm run dev

    # Clean build artifacts
    clean:
        find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
        find . -type d -name .pytest_cache -exec rm -rf {} + 2>/dev/null || true
        find . -type d -name .mypy_cache -exec rm -rf {} + 2>/dev/null || true
        find . -type d -name .ruff_cache -exec rm -rf {} + 2>/dev/null || true
        find . -type d -name node_modules -path "*/website/*" -exec rm -rf {} + 2>/dev/null || true
        find . -type d -name dist -exec rm -rf {} + 2>/dev/null || true
        echo "Cleaned build artifacts"
    ```
  </action>
  <verify>
    <automated>test -f Justfile && grep -q "just setup" Justfile && grep -q "just test" Justfile && grep -q "just lint" Justfile && grep -q "just format" Justfile && grep -q "just dev" Justfile</automated>
  </verify>
  <done>Justfile created with setup, test, lint, format, dev, dev-backend, dev-frontend, and clean commands</done>
  <acceptance_criteria>
    - Justfile exists in repository root
    - Justfile contains "setup:" recipe with uv sync and npm install
    - Justfile contains "test:" recipe with pytest and coverage
    - Justfile contains "lint:" recipe with ruff check and mypy
    - Justfile contains "format:" recipe with ruff format
    - Justfile contains "dev:" recipe that starts both backend and frontend
    - Justfile contains "dev-backend:" recipe with "uv run clerk web"
    - Justfile contains "dev-frontend:" recipe with "npm run dev"
    - Justfile contains "clean:" recipe for build artifacts
  </acceptance_criteria>
</task>

</tasks>

<verification>
- TOOL-01: UV workspace already exists (verified in root pyproject.toml)
- TOOL-02: Ruff configured in packages/clerk/pyproject.toml with line-length=100, target-version=py313
- TOOL-03: pytest configured with coverage reporting and 80% threshold
- TOOL-04: mypy configured with strict=true and python_version=3.13
- TOOL-06: Justfile provides setup, test, lint, format, dev commands
</verification>

<success_criteria>
- `grep -q "\[tool.ruff\]" packages/clerk/pyproject.toml`
- `grep -q 'line-length = 100' packages/clerk/pyproject.toml`
- `grep -q "\[tool.pytest.ini_options\]" packages/clerk/pyproject.toml`
- `grep -q 'fail_under = 80' packages/clerk/pyproject.toml`
- `grep -q "\[tool.mypy\]" packages/clerk/pyproject.toml`
- `grep -q 'strict = true' packages/clerk/pyproject.toml`
- `test -f Justfile && grep -q "just setup" Justfile`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
