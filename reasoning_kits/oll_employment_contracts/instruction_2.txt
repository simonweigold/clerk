You are conducting Swiss employment law research using OpenCaseLaw. Based on the contract and the initial contract analysis below, you must research Swiss employment law in the provided sources.

CONTRACT:
{resource_1}

INITIAL CONTRACT ANALYSIS:
{workflow_1}

AVAILABLE TOOLS:
You have access to the following Swiss legal research tools from OpenCaseLaw. You MUST invoke them by calling the tool function with the exact parameters. Do not merely describe what you would do — actually call the tools. Do not invent stuff and leave nothing out.

- {tool_1}: search_decisions — Search Swiss court decisions using full-text search. Parameters: query (string, required), canton (string, optional), court (string, optional), date_from (string, optional, YYYY-MM-DD), date_to (string, optional, YYYY-MM-DD), limit (integer, optional, default 10, max 50).
- {tool_2}: get_law — Look up the current authoritative text of any Swiss law article. Parameters: abbreviation (string, required, e.g. "OR"), article (string, required, e.g. "319"), language (string, optional, default "de").
- {tool_3}: find_leading_cases — Find the most-cited decisions for a topic or statute. Parameters: law_code (string, optional, e.g. "OR"), article (string, optional, e.g. "319"), topic (string, optional), limit (integer, optional, default 5).
- {tool_4}: get_doctrine — Get doctrinal commentary and Federal Council Botschaft for a Swiss law article. Parameters: abbreviation (string, required), article (string, required), language (string, optional, default "de").
- {tool_5}: search_laws — Full-text search across Swiss statute articles. Parameters: query (string, required), limit (integer, optional, default 10).
- {tool_6}: get_case_brief — Get a structured case brief for a Swiss court decision. Parameters: decision_id (string, required, e.g. "bger_4a_2023_123") OR reference (string, e.g. "BGE 143 III 123").

RESEARCH STRATEGY — follow this order:

Step A — Statute lookup (mandatory):
Call get_law for each of the following OR articles. These are required regardless of what the contract says:
- OR 319 (Einzelarbeitsvertrag definition)
- OR 324a (wage continuation during illness)
- OR 329a (minimum vacation)
- OR 335b (probation period and notice)
- OR 335c (notice periods)
- OR 336c (notice during protected periods)
- OR 340 and OR 340a (non-compete validity and limits)

Step B — Leading court decisions (mandatory):
Call find_leading_cases for EVERY mandatory article listed above. You must make at least 7 separate find_leading_cases calls — one per article:
  find_leading_cases(law_code="OR", article="319", limit=5)
  find_leading_cases(law_code="OR", article="324a", limit=5)
  find_leading_cases(law_code="OR", article="329a", limit=5)
  find_leading_cases(law_code="OR", article="335b", limit=5)
  find_leading_cases(law_code="OR", article="335c", limit=5)
  find_leading_cases(law_code="OR", article="336c", limit=5)
  find_leading_cases(law_code="OR", article="340", limit=5)

Each BGE decision returned becomes its own entry in the output JSON. Do not group them.

Also call search_decisions with 2–3 targeted keyword queries to find additional decisions (e.g. "Konkurrenzverbot Konventionalstrafe", "Sperrfrist Kündigung nichtig", "Probezeit Kündigung").

Step C — Additional research:
Look up any further articles, decisions, or statutes relevant to this specific contract's provisions.

OUTPUT FORMAT:
Produce a single JSON object. Each key is a source identifier, each value has exactly three fields: "retrieved_by", "source_type", "relevance".

- source_type "statute": entries from get_law calls. Key format: "OR Art. 319"
- source_type "case_law": entries from find_leading_cases or search_decisions. Key format: "BGE 130 III 28"

Example:
{
  "OR Art. 319": {
    "retrieved_by": "get_law(abbreviation='OR', article='319') — returned: statute text defining Einzelarbeitsvertrag",
    "source_type": "statute",
    "relevance": "Foundational article classifying this contract as an Einzelarbeitsvertrag, triggering all mandatory OR employment protections."
  },
  "BGE 130 III 28": {
    "retrieved_by": "find_leading_cases(law_code='OR', article='337', limit=5) — returned: citation count 412, summary of holding on wichtiger Grund",
    "source_type": "case_law",
    "relevance": "Leading case on immediate termination — relevant to the fristlose Auflösung clause in this contract."
  }
}

Rules:
- "retrieved_by": name the exact tool and parameters used, plus a brief summary of what it actually returned.
- Every entry must correspond to an actual tool call. Do not invent sources.
- Create one entry per source. Each BGE decision is its own entry with the BGE citation as the key.
