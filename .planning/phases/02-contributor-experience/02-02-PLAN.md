---
phase: 02-contributor-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .pre-commit-config.yaml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - TOOL-05
must_haves:
  truths:
    - Pre-commit hooks run automatically on git commit
    - Ruff linting runs before commit
    - mypy type checking runs before commit
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Pre-commit hooks configuration with Ruff and mypy"
      exports: []
    - path: "CONTRIBUTING.md"
      provides: "Updated with pre-commit setup instructions"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "packages/clerk/pyproject.toml"
      via: "shared Ruff and mypy configuration"
---

<objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-contributor-experience/02-CONTEXT.md
@packages/clerk/pyproject.toml (for Ruff and mypy versions)
@CONTRIBUTING.md (to append pre-commit instructions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pre-commit configuration</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - .planning/phases/02-contributor-experience/02-CONTEXT.md (D-04 for hook specifications)
    - packages/clerk/pyproject.toml (check Ruff version in dev dependencies, mypy version)
  </read_first>
  <action>Create `.pre-commit-config.yaml` in repository root with Ruff and mypy hooks per D-04.

Structure:
```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.0  # Match version in pyproject.toml dev dependencies
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.10.0  # Match version in pyproject.toml dev dependencies
    hooks:
      - id: mypy
        additional_dependencies: []  # Will be filled from pyproject.toml
```

Check pyproject.toml for exact versions of ruff and mypy in [project.optional-dependencies] dev section. Use those exact versions in the rev fields.

For mypy hook, the repository needs additional_dependencies for packages used by the code. Add common ones based on imports in the codebase (pydantic, sqlalchemy, etc.) or leave empty and let it use the installed environment.
  </action>
  <acceptance_criteria>
    - File `.pre-commit-config.yaml` exists in repository root
    - Contains repo: https://github.com/astral-sh/ruff-pre-commit
    - Contains repo: https://github.com/pre-commit/mirrors-mypy
    - Ruff version matches pyproject.toml dev dependencies
    - mypy version matches pyproject.toml dev dependencies
    - ruff hook has args: [--fix]
  </acceptance_criteria>
  <verify>
    <automated>test -f .pre-commit-config.yaml && grep -q "astral-sh/ruff-pre-commit" .pre-commit-config.yaml && grep -q "mirrors-mypy" .pre-commit-config.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Pre-commit config exists with Ruff and mypy hooks matching project versions</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-commit setup to CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (find section on setup)
    - .pre-commit-config.yaml (to reference in docs)
  </read_first>
  <action>Update CONTRIBUTING.md to add pre-commit installation instructions.

Find the development environment setup section in CONTRIBUTING.md. Add a subsection titled "Pre-commit Hooks" after the initial setup instructions.

Content to add:
```markdown
### Pre-commit Hooks

We use pre-commit to run linting and type checks before each commit.

1. Install pre-commit:
   ```bash
   uv pip install pre-commit
   ```

2. Install the hooks:
   ```bash
   pre-commit install
   ```

3. (Optional) Run on all files once:
   ```bash
   pre-commit run --all-files
   ```

The hooks will now run automatically on every commit, checking:
- Ruff linting and formatting
- mypy type checking
```

Insert this after the dev environment setup but before the "Making Changes" section (or create a logical flow if sections differ).
  </action>
  <acceptance_criteria>
    - CONTRIBUTING.md contains "Pre-commit Hooks" section
    - Contains "uv pip install pre-commit"
    - Contains "pre-commit install"
    - Mentions Ruff and mypy in the description
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Pre-commit Hooks" CONTRIBUTING.md && grep -q "pre-commit install" CONTRIBUTING.md && grep -q "Ruff" CONTRIBUTING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CONTRIBUTING.md documents pre-commit setup with installation and hook details</done>
</task>

</tasks>

<verification>
- `.pre-commit-config.yaml` exists with Ruff and mypy hooks
- Hook versions match pyproject.toml dev dependencies
- CONTRIBUTING.md has Pre-commit Hooks section with installation instructions
- Installation uses UV (consistent with project tooling)
</verification>

<success_criteria>
- [ ] Pre-commit config exists at `.pre-commit-config.yaml`
- [ ] Ruff and mypy hooks configured with correct versions
- [ ] CONTRIBUTING.md updated with setup instructions
- [ ] Instructions use `uv pip install pre-commit` (project-standard tooling)
</success_criteria>

<output>
After completion, create `.planning/phases/02-contributor-experience/02-02-SUMMARY.md`
</output>
